{% extends 'base.html.twig' %}

{% block body %}
<h1>🚨 Signalements</h1>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Annonce</th>
            <th>Auteur</th>
            <th>Signalé par</th>
            <th>Statut</th>
        </tr>
    </thead>
    <tbody>
        {% for report in reports %}
            <tr>
                <td>{{ report.id }}</td>
                <td><a href="{{ path('post_show', {id: report.post.id}) }}">{{ report.post.title }}</a></td>
                <td>{{ report.author.name }}</td>
                <td>{{ report.reporter.name }}</td>
                <td>{{ report.isHandled ? '✅ Traité' : '❌ Non traité' }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
