<style>
/* === NAVBAR CSS PUR === */
.navbar {
  background-color: #222;
  padding: 10px 20px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.navbar a {
  color: #f5f5f5;
  text-decoration: none;
  margin: 0 10px;
  font-weight: bold;
  transition: 0.3s;
}
.navbar a:hover {
  color: #ffcc00;
}
.navbar .left {
  display: flex;
  align-items: center;
}
.navbar .right {
  display: flex;
  align-items: center;
}
.navbar .search-box {
  margin-left: 20px;
}
.navbar .search-box input {
  padding: 5px;
  border-radius: 4px;
  border: none;
}
.navbar .search-box button {
  padding: 5px 10px;
  border: none;
  background: #ffcc00;
  cursor: pointer;
  border-radius: 4px;
  margin-left: 5px;
}
</style>

<nav class="navbar">
  <div class="left">
    <!-- Logo / Titre -->
    <a href="/">🏠 Bonne Affaire</a>

    <!-- Liens principaux -->
    <a href="{{ path('post_list') }}">Toutes les annonces</a>
    <a href="{{ path('post_new') }}">➕ Déposer une annonce</a>

    <!-- Liste des catégories -->
    <a href="{{ path('app_category_index') }}">📂 Catégories</a>

    <!-- Favoris -->
    <a href="{{ path('favorites_list') }}">⭐ Favoris</a>
  </div>

  <div class="right">
    <!-- Barre de recherche -->
    <form class="search-box" method="get" action="{{ path('post_search') }}">
      <input type="text" name="q" placeholder="Rechercher..." value="{{ term|default('') }}">
      <button type="submit">🔍</button>
    </form>

    <!-- Connexion / Déconnexion -->
    {% if app.user %}
      <a href="{{ path('app_logout') }}">🚪 Déconnexion</a>
    {% else %}
      <a href="{{ path('app_login') }}">🔑 Connexion</a>
    {% endif %}
  </div>
</nav>
