{% extends 'base.html.twig' %}

{% block title %}Résultats de recherche{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>🔍 Résultats pour "{{ term }}"</h1>

    <div class="row mt-3">
        {% for post in posts %}
            <div class="col-md-4 mb-3">
                <div class="card h-100 shadow-sm">
                    {% if post.image1 %}
                        <img src="{{ asset('uploads/' ~ post.image1) }}" class="card-img-top" alt="Image">
                    {% endif %}
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">{{ post.title }}</h5>
                        <p class="card-text">{{ post.price }} €</p>
                        <a href="{{ path('post_show', {id: post.id}) }}" class="btn btn-primary mt-auto">
                            Voir l'annonce
                        </a>
                        <a href="{{ path('favorites_add', {id: post.id}) }}" class="btn btn-warning">⭐ Ajouter aux favoris</a>
                                <a href="{{ path('report_post', {id: post.id}) }}" class="btn btn-danger">
                                🚩 Signaler cette annonce
                            </a>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <div class="alert alert-info">
                    Aucun résultat trouvé.
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
